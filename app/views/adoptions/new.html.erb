<h1>You want to adopt?</h1>

<%= form_for(@adoption) do |f| %>
  <%= f.collection_select(:user_id, User.all, :id, :username) %>
  <%= f.collection_select(:pet_id, Pet.all, :id, :name) %>
  <%= f.submit ("Adopt")%>
<% end %>
