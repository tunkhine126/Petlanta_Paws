<h1>Edit User</h1>
<%= form_for(@user) do |f| %>
<%= f.text_field(:username)%>
<%= f.email_field(:email)%>
<%= f.password_field(:password, placeholder: "Password")%>
<%= f.submit "Submit User Edit"%>
<%end%>
